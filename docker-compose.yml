services:
  app:
    build: .
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.my-app.rule=Host(`who.metehansenyer.tech`)"
      - "traefik.http.routers.app.entrypoints=web"
